T2 = 3E-9;
t = logspace(-13,-9,300);

P = 1/2*(1 + exp(-2*t/T2));
F = 1/2*(1 + exp(-t/T2));

figure;
loglog(t,1-P,t,1-F);
grid on
grid minor

%%
error = linspace(0.5,0.99,300);
nSteps = 15;
T2 = 10E-9;

P = error.^(1/nSteps);
t = -log(2*P - 1)/2*T2;

plot(error,t);
line([0.9 0.9], [min(t) y2]);
grid minor
grid on